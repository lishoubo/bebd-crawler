# BEBD 本地爬虫最小闭环 - Tasks

## Task 1 · 初始化轻量工程

- [x] 1.1 创建 Python 3.11+ 项目，依赖仅包含 DrissionPage、配置/模型及测试所需的最小集合
- [x] 1.2 建立 CLI、浏览器、认证模块；BEBD 采集适配器和输出模块留在 Task 4 实现
- [x] 1.3 创建 `.gitignore`，至少忽略 `.local/`、cookie 文件、浏览器用户目录和运行诊断文件
- [x] 1.4 提供不含凭证的品牌清单示例，初始仅包含“欧莱雅”

## Task 2 · 完成登录与 cookie 本地保存

- [x] 2.1 实现 `bebd-crawler login`，以可见浏览器打开目标页面并等待用户手工登录
- [x] 2.2 通过目标域/登录路由校验，并在真人验证中确认数据中心搜索框可访问
- [x] 2.3 通过 CDP 导出完整 cookie 属性，并原子写入 `.local/auth/bebd-cookies.json`
- [x] 2.4 保存后限制文件权限；日志中禁止输出 cookie 值
- [x] 2.5 实现 cookie 注入及会话校验；失效时给出明确的重新登录提示
- [x] 2.6 测试 cookie 序列化/反序列化、敏感字段不落日志及登录失效分支

## Task 3 · 登录后探查真实页面

- [x] 3.1 用户完成登录后，打开数据中心搜索页，确认路由及默认筛选状态
- [x] 3.2 按 `docs/踩点/搜索01.md` 确认“欧莱雅”使用顶部商品关键词搜索，而非品牌筛选
- [x] 3.3 拦截 XHR，确认 `goodsSearch` 接口、首屏参数、指数降序、总数和返回字段
- [x] 3.4 确认接口请求含动态签名，采用“页面交互 + 响应监听”方案；翻页选择器留给 Task 4
- [x] 3.5 保存不含请求头凭证的本地响应样例，并回填 `design.md`

## Task 4 · 实现“欧莱雅”单品牌采集

- [x] 4.1 实现品牌清单加载、关键词搜索和输入值校验
- [x] 4.2 实现 `goodsSearch` 响应监听和页面“下一页”驱动的分页采集器
- [x] 4.3 实现记录采集元数据、稳定键去重、逐页 JSONL 输出和运行摘要
- [x] 4.4 翻页前随机滚动、悬停与等待；页面返回后再次随机停顿
- [x] 4.5 增加响应解析、去重、逐页刷盘、品牌清单和人类化节奏单测
- [x] 4.6 `crawl` 每次启动可见的独立浏览器，并用 `finally` 保证成功、失败或中断后关闭
- [x] 4.7 数据请求后从浏览器回刷完整 cookie；仅在会话已验证有效时覆盖本地文件
- [x] 4.8 `crawl` 落到登录页时每 2 秒轮询等待手工登录，搜索页就绪后自动继续

## Task 5 · 真人验证

- [x] 5.1 执行一次手工登录并确认 cookie 文件已生成且位于 Git 忽略目录
- [x] 5.2 关闭并重新打开浏览器，确认 cookie 可恢复登录态
- [x] 5.3 完整采集“欧莱雅”前 90 条：9 页、90 条、90 个唯一 mid、接口总数 5533
- [x] 5.4 核对全局美修指数降序（88.93 → 52.58），保存 JSONL 和运行摘要
- [ ] 5.5 根据实测结果决定下一步：扩展品牌清单、增加详情采集，或再设计持久化层
